extends layout.pug

block content
    section#content
        if user 
            br
            a(href='/cards' class="button") Begin
        else    
            h2 Welcome, Student!
            a(href="http://localhost:4000/student") Sign up
