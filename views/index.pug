extends layout.pug

block content
    section#content
        if user 
            h2 Welcome, #{user}!
            form(action="/bye", method="post") 
                button(type="submit") Log Out 
        else    
            h2 Welcome, Student!
            a(href="http://localhost:4000/student") Sign up
