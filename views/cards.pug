extends layout.pug

block content
    section#content
        form(action="/cards", method="post")
            h3= prompt
            if hint 
                p 
                    i Hint: #{hint}
            h3 
                i Answer: #{answer}
            button(type="submit") Next