extends layout.pug

block content
    section#content
        h3= prompt
        if hint
            p 
                i Hint: #{hint}
        if answer
            h3 
                i Answer: #{answer}
        a(href=`${id}?side=${side}`)= showSide
        br 
        br
        a(href='/cards' class="button") Next