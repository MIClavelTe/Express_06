extends layout.pug

block content
    section#content
        if student 
            h2 Welcome, #{student}!
            a(href="http://localhost:4000/cards") Play  
            a(href="http://localhost:4000/student") GoodBye 
        else 
            h2 Welcome, Student!
            a(href="http://localhost:4000/student") Sign up