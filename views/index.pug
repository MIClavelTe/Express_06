extends layout.pug

block content
    section#content
        if user 
            h2 Welcome, #{user}!
            a(href="http://localhost:4000/cards") Play Now
        else    
            h2 Welcome, Student!
            a(href="http://localhost:4000/student") Sign up